services:
  spark-iceberg:
    image: tabulario/spark-iceberg
    container_name: spark-iceberg-scalable
    networks:
      scalable_net:
    depends_on:
      - rest
      - minio
    volumes:
      - ./warehouse:/home/iceberg/warehouse
      - ./notebooks:/home/iceberg/notebooks
      - ./data:/home/iceberg/data
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=${AWS_REGION}
    ports:
      - "127.0.0.1:8889:8888"
      - "127.0.0.1:8081:8080"
      - "127.0.0.1:10002:18080"
      - "127.0.0.1:4043-4045:4040-4042"

  rest:
    image: tabulario/iceberg-rest
    container_name: iceberg-rest-scalable
    networks:
      scalable_net:
    ports:
      - "127.0.0.1:8182:8181"
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=${AWS_REGION}
      - CATALOG_WAREHOUSE=${CATALOG_WAREHOUSE}
      - CATALOG_IO__IMPL=org.apache.iceberg.aws.s3.S3FileIO
      - CATALOG_S3_ENDPOINT=${CATALOG_S3_ENDPOINT}

  minio:
    image: minio/minio
    container_name: minio-scalable
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    networks:
      scalable_net:
        aliases:
          - warehouse.minio
    ports:
      - "127.0.0.1:9101:9001"
      - "127.0.0.1:9100:9000"
    command: ["server", "/data", "--console-address", ":9001", "--address", ":9000"]

  mc:
    depends_on:
      - minio
    image: minio/mc
    container_name: mc-scalable
    networks:
      scalable_net:
    environment:
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=${AWS_REGION}
    entrypoint: >
      /bin/sh -c "
      until (/usr/bin/mc config host add minio ${CATALOG_S3_ENDPOINT} ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD}) do echo '...waiting...' && sleep 1; done;
      /usr/bin/mc mb --ignore-existing minio/warehouse;
      /usr/bin/mc mb --ignore-existing minio/bronze;
      /usr/bin/mc mb --ignore-existing minio/silver;
      /usr/bin/mc mb --ignore-existing minio/gold;
      /usr/bin/mc policy set public minio/warehouse;
      /usr/bin/mc policy set public minio/bronze;
      tail -f /dev/null
      "

networks:
  scalable_net:
    driver: bridge