name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # 01:00 AM UTC daily.
    - cron: '0 01 * * *'

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Added 'schedule' to the allowed event names
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'schedule')
    
    steps:
      - name: Print Pipeline Status
        run: |
          echo "Triggered by: ${{ github.event_name }}"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Connect to Tailscale
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_CLIENT_SECRET }}
          tags: tag:github-deployer

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }} 
          username: ${{ secrets.DEPLOYER_USER }}
          key: ${{ secrets.DEPLOYER_SSH }}
          script: |
            set -ex
            cd ${{ secrets.FOLDER_PATH }}
            
            if [ "${{ github.event_name }}" = "push" ]; then
              git pull origin main
            fi
            
            # Ensure infrastructure is up (binds to localhost per your config)
            docker-compose up -d
            
            # Run the polyglot pipeline
            make zap
            
            docker image prune -f